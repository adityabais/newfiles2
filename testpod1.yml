kind: Pod
apiVersion: v1
metadata:
  name: newpod1
  labels:
    env: developments
    class: pods 
spec:
  containers:
    - name: c00
      image: ubuntu
      command: ["/bin/bash", "-c", "while true; do echo hello-aditya; sleep 5; done"]
